apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: artifactory-mystory
  namespace: flux-system
spec:
  interval: 5m
  url: http://artifactory-oss.artifactory/artifactory/mystory-virtual
  secretRef:
    name: artifactory-mystory-credentials

