apiVersion: v1
kind: Service
metadata:
  name: artifactory
spec:
  type: ClusterIP
  selector:
    app: artifactory
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
