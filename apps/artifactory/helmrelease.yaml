apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: artifactory-oss
  namespace: artifactory
spec:
  interval: 5m
  chart:
    spec:
      chart: artifactory-oss
      version: "2.1.0"
      sourceRef:
        kind: HelmRepository
        name: jfrog
        namespace: flux-system
  values: {} # use all chart defaults
  