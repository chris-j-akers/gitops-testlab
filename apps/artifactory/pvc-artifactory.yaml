apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-artifactory
  namespace: artifactory
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-artifactory
  storageClassName: ""
